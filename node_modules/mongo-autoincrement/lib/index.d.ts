import { Db, ClientSession } from 'mongodb';
interface Options {
    collectionName?: string;
    step?: number;
    filter?: object;
    session?: ClientSession;
}
declare function autoIncrement(db: Db, collection: string, field: string, { filter, collectionName, step, session }?: Options): Promise<number>;
export = autoIncrement;
